sed -i '/if (state.src) win.__globalBgAudio.src = state.src;/d' src/components/common/MusicPlayer.astro
sed -i 's/if (state.currentTime > 0) {/if (state.currentTime > 0) {\n            const targetTime = state.currentTime;\n            const onLoaded = () => {\n              if (win.__globalBgAudio) win.__globalBgAudio.currentTime = targetTime;\n              win.__globalBgAudio?.removeEventListener('\''loadedmetadata'\'', onLoaded);\n            };\n            win.__globalBgAudio.addEventListener('\''loadedmetadata'\'', onLoaded);\n          }\n          if (state.src) win.__globalBgAudio.src = state.src;/g' src/components/common/MusicPlayer.astro
